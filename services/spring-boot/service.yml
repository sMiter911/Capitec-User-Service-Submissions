apiVersion: v1
kind: Service
metadata:
  name: spring-boot-service
  labels:
    name: spring-boot-user-service
    app: capitec-user-service
spec:
  selector:
    name: spring-boot-user-service
    app: capitec-user-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30221
  type: NodePort